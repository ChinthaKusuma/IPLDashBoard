import './index.css'

const NotFound = props => {
  // eslint-disable-next-line
  const {item} = props
  return (
    <div className="not">
      <h1>Page Not Found</h1>
    </div>
  )
}
export default NotFound
